setup:
	- the whole UI
		- bucket properties
		- auto focus for adding blocks/buckets
	
editor (ckeditor):
	
loader.js
	- allow configuration to determine loader behaviour (i.e. async, overwrite behaviour)

Admin header - make responsive to window width

Live Edit:
	CKEditor load time
	if no live url defined, don't allow user to edit